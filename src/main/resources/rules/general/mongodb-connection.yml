kind: RegexRule
metadata:
  id: mongodb-connection-string
  name: MongoDB Connection String
  category: database
  severity: CRITICAL
  description: Detect MongoDB connection strings with credentials
spec:
  pattern: "mongodb(?:\\+srv)?://[^\\s:]+:[^\\s@]+@[A-Za-z0-9._\\-]+(:[0-9]+)?"
