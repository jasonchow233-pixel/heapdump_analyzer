kind: RegexRule
metadata:
  id: smtp-credential
  name: SMTP Credential
  category: general
  severity: HIGH
  description: Detect SMTP credentials in string pool (host:port with auth)
spec:
  pattern: "(?i)smtp://[^\\s:]+:[^\\s@]+@[A-Za-z0-9._\\-]+(:[0-9]+)?"
