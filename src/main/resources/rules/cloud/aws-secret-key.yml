kind: RegexRule
metadata:
  id: aws-secret-access-key
  name: AWS Secret Access Key
  category: cloud
  severity: CRITICAL
  description: Detect AWS Secret Access Key values in heap string pool
spec:
  pattern: "(?i)aws[_-]?secret[_-]?access[_-]?key\\s*[=:]\\s*[A-Za-z0-9/+=]{40}"
